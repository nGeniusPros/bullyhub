'use client';

import { ProductList } from '@/features/marketplace';

// Metadata must be in a separate file or removed when using 'use client'

export default function MarketplacePage() {
  return <ProductList />;
}
