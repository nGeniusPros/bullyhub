'use client';

import { ShoppingCart } from '@/features/marketplace';

// Metadata must be in a separate file or removed when using 'use client'

export default function CartPage() {
  return <ShoppingCart />;
}
